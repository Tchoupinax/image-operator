{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.17.0
  name: imagebuilders.tchoupinax.dev
spec:
  group: tchoupinax.dev
  names:
    kind: ImageBuilder
    listKind: ImageBuilderList
    plural: imagebuilders
    singular: imagebuilder
  scope: Namespaced
  versions:
  - name: v1beta
    schema:
      openAPIV3Schema:
        description: ImageBuilder is the Schema for the imagebuilders API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageBuilderSpec defines the desired state of ImageBuilder.
            properties:
              foo:
                description: Foo is an example field of ImageBuilder. Edit imagebuilder_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: ImageBuilderStatus defines the observed state of ImageBuilder.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
